---
page_title: "fastly_ngwaf_workspace_alert_microsoft_teams_integration Resource - fastly"
subcategory: ""
description: |-
  Manages a Fastly Next-Gen WAF Microsoft Teams alert integration scoped to a single workspace.
---

# fastly_ngwaf_workspace_alert_microsoft_teams_integration (Resource)

Manages a Fastly Next-Gen WAF `microsoftteams` alert integration scoped to a single workspace.

Workspace alert integrations are versionless and belong to an NGWAF workspace, not to a Fastly service version. Alert integrations of other types are managed by their own `fastly_ngwaf_workspace_alert_*_integration` resources.

## Example Usage

```terraform
resource "fastly_ngwaf_workspace" "example" {
  name        = "Example Workspace"
  description = "Managed by Terraform"
  mode        = "block"

  attack_signal_thresholds {}
}

resource "fastly_ngwaf_workspace_alert_microsoft_teams_integration" "example" {
  workspace_id = fastly_ngwaf_workspace.example.id
  description  = "Managed by Terraform"

  webhook      = "https://example.com/webhooks/my-service"
}
```

{{ .SchemaMarkdown | trimspace }}

## Import

Next-Gen WAF workspace microsoftteams alert integrations are imported using a combination
of the workspace ID and alert integration ID, separated by a `/`:

```shell
terraform import fastly_ngwaf_workspace_alert_microsoft_teams_integration.example WORKSPACE_ID/ALERT_ID
```
