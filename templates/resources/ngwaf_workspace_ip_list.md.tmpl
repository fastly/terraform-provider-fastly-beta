---
page_title: "fastly_ngwaf_workspace_ip_list Resource - fastly"
subcategory: ""
description: |-
  Manages a Fastly Next-Gen WAF ip list scoped to a single workspace.
---

# fastly_ngwaf_workspace_ip_list (Resource)

Manages a Fastly Next-Gen WAF `ip` list scoped to a single workspace.

Workspace lists are versionless and belong to an NGWAF workspace, not to a
Fastly service version. This resource manages lists whose entries are IP address or CIDR values.

Lists of other types are managed by their own resources:

- `fastly_ngwaf_workspace_ip_list`
- `fastly_ngwaf_workspace_string_list`
- `fastly_ngwaf_workspace_wildcard_list`
- `fastly_ngwaf_workspace_country_list`
- `fastly_ngwaf_workspace_signal_list`

All five can be read back together via the `fastly_ngwaf_workspace_lists` data
source.

## Example Usage

```terraform
resource "fastly_ngwaf_workspace" "example" {
  name        = "Example Workspace"
  description = "Managed by Terraform"
  mode        = "block"

  attack_signal_thresholds {}
}

resource "fastly_ngwaf_workspace_ip_list" "example" {
  workspace_id = fastly_ngwaf_workspace.example.id
  name         = "Example IP List"
  description  = "Managed by Terraform"

  entries = ["10.0.0.1", "192.168.0.0/24"]
}
```

{{ .SchemaMarkdown | trimspace }}

## Import

Next-Gen WAF workspace ip lists are imported using a combination of the
workspace ID and the list ID, separated by a `/`:

```shell
terraform import fastly_ngwaf_workspace_ip_list.example WORKSPACE_ID/LIST_ID
```
